if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/cmake")
    include(cmake/${PLATFORM}.cmake OPTIONAL)
    include(cmake/${BACKEND}.cmake OPTIONAL)
else()
    include(cmake_external/${PLATFORM}.cmake OPTIONAL)
endif()
